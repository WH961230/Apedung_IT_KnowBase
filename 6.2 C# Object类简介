Object 类是 C# 语言中最原始、最重要的类，是所有类的“祖先”，
每个 C# 类都是它的子类，它实现了每个类都必须具有的基本方法。

>> 祖先是 Object 披着兽皮的祖先 是代码家族的鼻祖 每个人都是他的子代 全世界最占便宜的人！ 他身上的品质我们每个人都有被传承。

Object 类中的属性和方法可以被用到任何类。

>> 祖先就是没有之上，只有之下，他的品质可以被用在任何一个子代中

当編程者定义一个类时没有使用关键字 Extends 指明它的父类，则編译器认为该类从 Object 类继承而来。

>> 默认继承Object 上帝论 这个我不喜欢。西方的话把他称作上帝

在 Object 类中提供了 4 个常用的方法，即 Equals、GetHashCode、GetType 以及 ToString 方法。


关键点：没什么用的祖先 Object 类。
